require "spec_helper"

RSpec.describe Omniauth::Acuity do
  it "has a version number" do
    expect(Omniauth::Acuity::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
